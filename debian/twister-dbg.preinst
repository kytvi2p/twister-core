#!/bin/sh

set -e

  if [ ! -z $2 ]; then
      if dpkg --compare-versions $2 lt 0.9.26.0+git20141016.94d29f8-2; then
          test -d /usr/share/doc/twister-dbg && rm -rf /usr/share/doc/twister-dbg
      fi
  fi

#DEBHELPER#

